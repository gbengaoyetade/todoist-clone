version: 2
jobs:
  build:
    working_directory: ~/app
    docker:
      - image: circleci/node:11.12.0
      - image: mongo:4.0.3
    steps:
      - checkout
      - run: npm install
      - run: npm test
